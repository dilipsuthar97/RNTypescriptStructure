import * as React from 'react';

import Routes from './Routes';

interface Props {}

const App: React.FC<Props> = () => {
  return (
    <React.Fragment>
      <Routes />
    </React.Fragment>
  );
};

export default App;
